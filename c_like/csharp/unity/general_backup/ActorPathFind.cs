using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * ActorPathFind : Component to determine movement from
 * an actors position to a specified room.
 * Operations:
 * 1) Check Actor is on correct floor, if not, walk to an elevator.
 * 2) If actor is on correct floor: Determine direction, move in that direction.
 */
public class ActorPathFind : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
