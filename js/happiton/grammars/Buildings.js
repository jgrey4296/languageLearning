if(typeof define !== 'function'){
    var define = require('amdefine')(module);
}

define([],function(){
    return {
        "building" : "${type}",
        "type" : "Firestation Shop Convent Church Parish Farm Printers Newsagents".split(" ")
    };
});
