if(typeof define !== 'function'){
    var define = require('amdefine')(module);
}

define([],function(){

    var RoutineElement = function(){
        this.action = null;
    };

    return RoutineElement;
});
